window.addEventListener('load', () => {
    // const wppLogo = document.querySelector('.whatsapp-link');
    // var prevScrollpos= window.pageYOffset;;

    // // Discrimino por medidas de pantalla
    // if (window.innerWidth < 1024) { // Mobile & Tablet
    //     window.addEventListener('scroll', function () { //En mobile
    //         // prevScrollpos 
    //         if (window.innerHeight + window.pageYOffset >= document.body.scrollHeight) { //Llego al final
    //             console.log('Llegaste al final');
    //             wppLogo.classList.add('whatsapp-link-active');
    //             var currentScrollPos = window.pageYOffset;
    //             if (prevScrollpos > currentScrollPos) { //Hizo scroll up
    //                 console.log('Has hecho scroll hacia arriba desde el final');
    //                 // Aquí puedes agregar el código que deseas ejecutar cuando el usuario hace scroll hacia arriba
    //             }
    //             prevScrollpos = currentScrollPos;
    //         }
    //     });

    // } else {
    //     window.addEventListener('scroll', function () { //En desktop
    //         if (window.scrollY + window.innerHeight >= document.documentElement.scrollHeight) {
    //             console.log('Has llegado al final de la página');
    //             // Aquí puedes agregar el código que deseas ejecutar cuando el usuario llega al final de la página
    //         }
    //     });

    // }

});